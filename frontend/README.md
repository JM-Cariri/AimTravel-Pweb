# AimTravel Frontend

Instalar dependências e rodar em desenvolvimento (na pasta `frontend`):

```bash
npm install
npm run dev
```

A aplicação espera a API disponível em `http://localhost:3001/api`.
